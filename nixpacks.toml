# Usa o build/start padrao do Nixpacks PHP; so ajusta document root e fallback.
# Nao defina NIXPACKS_PHP_ROOT_DIR / NIXPACKS_PHP_FALLBACK_PATH no Coolify.

[variables]
NIXPACKS_PHP_ROOT_DIR = "public"
NIXPACKS_PHP_FALLBACK_PATH = "/index.php"
